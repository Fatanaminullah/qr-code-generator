[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (so refresh/deep-links keep working)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"


